const fa = require('fs');
const { Op } = require('sequelize');
const { TransactionType, Transaction, User, sequelize } = require('../models');

exports.createTransaction = async (req, res, next) => {
    try {
        const { transac,   }
    } catch (err) {
        next(err);
    }
}